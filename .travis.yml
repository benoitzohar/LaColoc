language: node_js
sudo: false
node_js:
  - "node"
services:
  - mongodb
env:
  - CXX=g++-4.8
script:
  - yarn test:check-coverage
after_script:
  - yarn report-coverage
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
